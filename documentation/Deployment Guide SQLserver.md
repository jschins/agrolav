# Deployment

Superseded by **`deployment.md`** (same production host,
`https://expenses.apsurt.nl`, SQL Server in Docker, systemd hub + client,
Caddy).

Use that file. Do not copy passwords into markdown.
