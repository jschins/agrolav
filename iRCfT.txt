AGENDA

1. At creating a new country: 
additionally make, in table dbo.dim_categories, a third row
(country_id * 100 + 2, country_id, 97, '18 UFO', 'True', NULL)

2. SHOW modify_categories menu option only for national login and make it a knob in 8200 and have it refer thereto (like add-person, accessible to national and local logins)
i think the add-person option was broke

3. At modifying categories in the menu, such that previously existing catogory_id's cease existing:
additionally, eliminate all rows in table dbo.category_term that have the eliminated category_id
(this is the first operation in a sequence, the last one of which is the elimination of that category_id in the table dbo.dim_category)
Moreover, require that any new category be structured as 2digits+space+character_sequence such that 2digits (==local_code) represent unique number [omit semantic requirement that character_sequence be unique within country]
NOTE that 2digits and char_seq must be input and stored independently
NOTE that SUBMIT can reply with an error, upon which user has choice to cancel or continue

4. replace secret -> ing in dbo.account.format, and add columns of now table dbo.enable_account
          "connection_id" (FK referring to table dbo.enable_connection) 
          "uid": "1f60d580-f565-4cb2-a6bd-c285d818bfee",
          "identification_hash": "WwpbCiJhY2NvdW50IiwKImFjY291bnRfaWQiLAoiaWJhbiIKXSwKWwoiYWNjb3VudCIsCiJjdXJyZW5jeSIKXQpd.gz/T5pnwk7aZ/7C6kMmw8RDx5+RnPLQO3zF9rCxWNdk=",
NOTE that ing = apsp
IDEA is that 
+ every account has its own uid and hash
+ several accounts can connect via a single connection_id
+ CONSTRAINT is that equal connection_id's on different accounts require those accounts to belong to the same person

5. table dbo.enable_connection contains the columns
      connection_id (PK)
      "app_id": "fb529b7f-9655-4b21-9589-6054459bde25",
      "session_id": "08761cd8-ffdf-461f-9b8c-ceda2ed8f11d",
      "valid_until": "2026-11-08T15:29:51.524206Z",
      "created_at": "2026-08-10T15:30:41.276604+00:00",
      "pem":  pem-string 
NB app_id and pem from dbo.private_key, erroneously LINKED TO person_id
IDEA is that one person can have some accounts under pem, and some others summarized in a manually uploaded excel document
DELETE dbo.private_key

6. User's guide:
+ 18 reserved for UFO
+ priority rules: type_rules, P-G, &&, last in categoric-alphabetical order
+ application: either name or description
+ right-clicking on amount or term in description/name
+ blue and editing in description
+ categories per country, P and G, 

7. add split amount option: right-clicking on amount gives menu

8. change password to !@#$%^&*()_{login_name}

9. add column dbo.hub_ip.target
+ if its value == 'B': refers to ip's with access to 8200
+ if its value == C_{country_id} or L_{center_id} or P_{person_id} then the value limits access to that login on 8300 
IDEA is that multiple IP's may be assocaiated with a single login
so at login, code should check if IP is one of all IP's associated with that login

10. add 8200 knob and menu item "restrict IP access" visible to everybody: if no IP address is specfied, access is possible from everywhere

11. can you read the pem file content & name wthout committing it to disk?



================================================================================================




i introduced in all three transactions tables, a column "modification" initialized at M = -1

furthermore, i deleted the column "cat_id_set" and renamed the column "cat_id_calculated" to "category_id"

the meaning of the possible values of modification:
M=-1: the categories have never been calculated (this is the initial situation, when banking statements have just been downloaded from the bank and uploaded into the database)
for M >=0, the categories have been calculated
M=0:  the user did not modify the transaction by hand (neither category, nor description)
M=1:  the user modified only the category
M=2:  the user modified only the description
M=3:  the user modified both

for both a manual category or description modification, the existing values are overwritten
this was previously the case only for the description
not for category: that value ended up in cat_id_set
however, now i want this to change
the category_id value is simply overwritten , but the manual change is reflected in the value of manual

consequences of M value on CSS:
M=1: category value shows bold in the table
M=2: full transaction line shows blue in the table
M=3: both


i want to introduce the concept of "incremental Recalculation of Categories for Term (iRCfT)"

1. to this end, introduced the column transaction.hit VARCHAR(64) NULL
the value registers the category-determining term, according to the priority rule set

2.1. A Personal Term beats all General Terms
2.2. && beats non-&&
2.3. a "later term" beats an "earlier term" in categorical-alphabetical ordering

the latter simply means that
+ on calculating the category, all the terms in the G- and P-tables are applied to the concatenation of the Name and Description fields one by one
+ everytime a hit is found, it overwrites the category for that hit, and the term in transaction.hit, unless the priority rules forbid it
+ the third rule then means that, if the first two rules are heeded, and a later applied term gives a hit, the earlier registered category is simply overwritten , i.e., the last value sticks

the concept of "incremental Recalculation of Categories for Term (iRCfT) means that
+ whenever a NEW TERM IS ADDED to the G or P-table, all transactions are recalculated one by one for this newly added term only;
  a transaction is not recalculated
  -  when its category was hand-set (M=1 or M=3)
  -  when the hit term has priority over the newly added term
+ whenever an OLD TERM IS DELETED from the G- or P-table, only those transactions are recalculated, which show that term in the hit column


this is meant to speed up calculations
does this make sense to you?


1. when new data have been downloaded, automatically calculate the categories for only those data
2. add a "recalculate from scratch" button on the top bar, to the right of the dropdown menus and to the left of the logout button
when the user hits this button, the categories of all transactions for all countries are recalculated
during the calculation, present a grey overlay screen with a message on top, like in the transaction-download case



a final ingredient to give the user the idea of fast frontend response:
+ when the user modifies a category in Transactions-view, the frontend autonomously deletes the corresponding line and shows the updated table, whithout returning to Matrix-view, after which the backend starts iRCfT 
+ when the user modifies a description in Transactions-view, the frontend autonomously colors the line blue and shows the updated table, whithout returning to Matrix-view
+ when the user adds/modifies a term in Terms-view, the view remains the same, after which the backend starts iRCfT
+ when the user adds a term in right-click pop-up, the frontend autonomously deletes the corresponding line and shows the updated table, whithout returning to Matrix-view, after which the backend starts iRCfT

does this make sense to you?

NB rename folder agrolav -> expenses
